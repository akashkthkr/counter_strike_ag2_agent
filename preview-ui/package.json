{
  "name": "counter-strike-ag2-preview",
  "version": "1.0.0",
  "description": "Live preview UI for Counter-Strike AG2 Agent System",
  "main": "index.js",
  "scripts": {
    "build": "npm run copy-assets",
    "copy-assets": "mkdir -p dist && cp -r ../static/* dist/ && cp -r ../templates/* dist/",
    "dev": "python -m http.server 8080",
    "preview": "npx serve dist"
  },
  "keywords": ["counter-strike", "ag2", "agents", "game", "ai"],
  "author": "Your Name",
  "license": "MIT",
  "devDependencies": {
    "serve": "^14.0.0"
  }
}
